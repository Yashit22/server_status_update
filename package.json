{
  "name": "server-status-update",
  "version": "1.0.0",
  "description": "Vercel cron health-check for production Linux server tunnels, with Resend email alerts.",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "resend": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@vercel/node": "^5.0.0",
    "typescript": "^5.7.0",
    "vercel": "^39.0.0"
  }
}
